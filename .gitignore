# Maven build output
target/

# Maven Wrapper scripts and related files
mvnw
mvnw.cmd
# Maven wrapper jar - 명시적으로 추적 (다른 *.jar 규칙이 있어도 이것은 추적함)
# 만약 mvnw, mvnw.cmd 를 무시한다면, 아래 설정도 함께 무시하거나 주석처리 할 수 있습니다.
# 현재는 사용자의 이전 요청대로 wrapper.jar는 추적하도록 ! 규칙을 유지합니다.
!/.mvn/wrapper/maven-wrapper.jar

# SQL script files (필요에 따라 더 구체적인 패턴 사용 가능)
dummy_reports.sql
# 또는 모든 .sql 파일을 무시하고 싶다면:
# *.sql

# Log files
*.log

# Runtime data
pids/
*.pid
*.seed
*.out

# Temporary files generated by editors or OS
*~
*.tmp
*.bak
*.swp
._*

# IDE settings
# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# Eclipse & STS
.classpath
.project
.settings/
.springBeans
.sts4-cache
.apt_generated
.factorypath

# VS Code
.vscode/

# NetBeans (기본적인 것들, 필요시 추가)
nbproject/private/
build/
nbbuild/
dist/
nbdist/
.nb-gradle/

# Operating System generated files
.DS_Store
Thumbs.db

# 사용자 지정 무시 파일 (기존 .gitignore에서 가져옴)
HELP.md

# 중요: application.yaml 파일에 민감 정보(DB 접속 정보, API 키 등)가 있다면
# .gitignore 에 추가하여 커밋되지 않도록 해야 합니다.
# 예시:
# src/main/resources/application.yaml
# 현재는 주석 처리되어 있으므로, 필요에 따라 주석을 해제하고 사용하세요.