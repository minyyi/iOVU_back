# Maven build output
target/

# Maven wrapper jar - 명시적으로 추적 (다른 *.jar 규칙이 있어도 이것은 추적함)
# .mvn 디렉토리 자체가 무시되지 않는다고 가정
!/.mvn/wrapper/maven-wrapper.jar

# Log files
*.log

# Runtime data
pids/
*.pid
*.seed
*.out

# Temporary files generated by editors or OS
*~
*.tmp
*.bak
*.swp
._*

# IDE settings
# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# Eclipse & STS
.classpath
.project
.settings/
.springBeans
.sts4-cache
.apt_generated
.factorypath

# VS Code
.vscode/

# NetBeans (기본적인 것들, 필요시 추가)
nbproject/private/
build/
nbbuild/
dist/
nbdist/
.nb-gradle/


# Operating System generated files
.DS_Store
Thumbs.db

# 사용자 지정 무시 파일 (기존 .gitignore에서 가져옴)
HELP.md

# 중요: application.yaml 파일에 민감 정보(DB 접속 정보, API 키 등)가 있다면
# .gitignore 에 추가하여 커밋되지 않도록 해야 합니다.
# 예시:
# src/main/resources/application.yaml
# 현재는 주석 처리되어 있으므로, 필요에 따라 주석을 해제하고 사용하세요.
# 제공해주신 tree 에는 src/main/resources/application.yaml 이 있고,
# 현재 .gitignore 에서는 #src/main/resources/application.yaml 로 주석처리 되어있어 추적 중입니다.
# 이 파일에 민감 정보가 없다면 그대로 두셔도 됩니다.